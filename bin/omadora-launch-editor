#!/bin/bash
# Launch preferred code editor

if command -v code &>/dev/null; then
  code
elif command -v codium &>/dev/null; then
  codium
elif command -v kate &>/dev/null; then
  kate
else
  konsole -e nvim
fi
